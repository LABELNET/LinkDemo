<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yuan.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RqcodeActivity" />
        <activity android:name=".UserIfoActivity" />
        <activity android:name=".UpdateIfoActivity" />
        <activity android:name=".SafetyActivity" />
        <activity android:name=".SafetyPhoneActivity" />
        <activity android:name=".SafetyPasswordActivity"></activity>
    </application>

</manifest>